# FASHION-MNIST-CNN
Built a Convolutional Neural Network (CNN) using TensorFlow and Keras to classify Fashion MNIST images. The project includes data preprocessing, image normalization, CNN model training, evaluation, and saving the trained model architecture and weights.
